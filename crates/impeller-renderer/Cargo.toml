[package]
name = "impeller-renderer"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
readme = "README.md"
publish.workspace = true
license.workspace = true
repository.workspace = true
description = "Render pass encoding for impeller-rs, generic over the rendering HAL."
keywords = ["renderer", "graphics", "render-pass", "2d"]
categories = ["graphics", "rendering"]

[lints]
workspace = true

[dependencies]
glam = { workspace = true }
impeller-geometry = { workspace = true }
impeller-hal = { workspace = true }

[dev-dependencies]
# A concrete backend is needed to check that geometry lands on the pixels it
# should. This is a test-only dependency; the renderer itself stays generic.
impeller-hal-vulkan = { path = "../impeller-hal-vulkan" }
