[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
# This workspace's own tooling -- the gate, the bench, the report. It is a
# development dependency of the repository rather than of any caller, and it would
# be the only crate here that publishes a binary nobody outside asked for.
publish = false
license.workspace = true

[lints]
workspace = true

[dependencies]
impeller-core = { workspace = true }
impeller-hal = { workspace = true }
impeller-hal-vulkan = { workspace = true }
impeller-hal-gles = { workspace = true }
impeller-present-drm = { workspace = true }
impeller-testkit = { workspace = true }
glow = { workspace = true }
