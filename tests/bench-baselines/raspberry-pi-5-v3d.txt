# A Raspberry Pi 5, Mesa V3D 7.1.7.0, release build, fan fitted, idle at 57 C,
# `vcgencmd get_throttled` clean before and after.
#
#   cargo xtask bench --skip llvmpipe --check tests/bench-baselines/raspberry-pi-5-v3d.txt
#
# `--skip llvmpipe` because measuring the software rasterizer locks this board
# up; see docs/on-a-board.md.
#
# Checked against three fresh runs on the board it came from: all eight rows
# within three tenths of a percent in every one of them, which is what says this
# board can gate at all.
#
# Updating this is a reviewed event, like the shader snapshots: re-record with
# --record, and the diff is the claim that every row moved for a reason. The
# GLES rows repeat to about a tenth of a percent and mean what they say.
#
# All four *Vulkan* rows land in one of two states from one process to the next,
# and that sentence used to name only the distance-field one. Measured
# 2026-09-01 over sixteen runs across three commits: the four move together,
# every run is wholly in one state or the other, and the gap is 4.3 per cent on
# the distance field, 0.8 and 1.1 on the tessellated pair and 1.6 on the frame.
# So the explanation the older note gave -- the cost of recording a hundred and
# sixty draws -- cannot be the whole of it, since the tessellated rows submit
# one draw and move too. What it is has not been established from here.
#
# Each of those four therefore carries a fourth field, its own tolerance, wide
# enough to cover the other state. They are recorded in the faster one, which
# is the state to compare a change against; the tolerance is what keeps a run
# that landed in the slower one from reading as a regression. Before this the
# three unnamed rows had the default one per cent and failed on any slow-mode
# run, which is noise this file was producing rather than catching.
#
# Last checked against the board: b284c3e
#
# That line names the commit whose state a board run passed against, which is
# never the commit that writes the line -- a hash cannot be recorded inside the
# object it names. So it is updated afterwards, in a commit that touches nothing
# the bench times, and the count below it stays honest.
#
# It is read by `cargo xtask gate`, which counts the commits touching what the
# bench times since it and says so. It exists because the numbers below
# once went sixteen renderer commits unchecked and were ten and a half per cent
# out, and nothing in the tree could have said so -- "I ran it" is not a fact a
# later reader can find. Update it when a `--check` run passes, whether or not
# the numbers are re-recorded.
#
# Tolerances re-derived on 2026-09-18 from ten runs with the governor pinned, and
# the numbers below are untouched. Five rows tighten and one widens, which is what
# ten runs say rather than what caution said.
#
# **The Vulkan bimodality was the frequency governor.** Ten pinned runs, and all
# eighteen rows land in one cluster each: the distance field spreads 0.14 per cent
# where it used to alternate between 8.85 and 9.22, and no row shows a gap with
# nothing in it. Nine unpinned runs had shown the slow state five times. Taken with
# the mechanism -- 1.5 GHz against 2.4, a factor that contains every gap ever
# observed here, and pinning removes it -- that is established rather than
# suspected, which the paragraphs further down were careful not to claim.
#
# Two mechanisms were recorded here before this and both were wrong: heat, tested
# with a fan and a cold boot, and accumulated session state, tested with a power
# cycle. This one was tested by removing it.
#
# So the four Vulkan tolerances that existed *only* to cover the slow state come
# down, and that matters more than tidiness. The shader regression found on the same
# day was 2.35 per cent on the distance field, state for state -- **under the 5.0
# that row carried.** It failed its check only because that run happened to land in
# the slow state and read 6.3 per cent. A tolerance wide enough to hold two states
# is wide enough to hide the regression it is there to catch, and this file was
# doing both at once. At 2.0 that change fails on its merits.
#
# Sized at five to ten times the worst deviation seen over the ten runs:
#
#   vulkan distance field   0.14%  ->  2.0   was 5.0
#   vulkan stroked field    0.15%  ->  2.0   was 5.0
#   vulkan full frame       0.37%  ->  2.0   was 2.5
#   vulkan tessellated x4   0.20%  ->  2.0   unchanged
#   vulkan tessellated x1   0.30%  ->  2.0   unchanged
#   vulkan stroked path     1.77%  ->  3.0   was 2.0, and this one widens
#   gles stroked field      0.11%  ->  default, was 5.0
#   gles everything else    under 0.7%, all at the default already
#
# The Vulkan stroked path is the one row that needed more rather than less, and it
# is the cheapest frame in the file at 1.2 ms -- so the fixed cost of getting a
# frame started is a larger fraction of it than of anything else, and that is what
# moves. Recorded rather than smoothed over, because a row that widens in the same
# commit as five that narrow is the evidence that the numbers were read.
#
# The recording rows keep 5.0, and the frame's build keeps 10.0: their worst
# deviations were 1.35 per cent and one unit of the thousandth this file stores.
#
# Re-recorded a sixth time on 2026-09-18, for the `recording` section and for a
# board setting the numbers turn out to depend on.
#
# That section is what building each frame costs, before anything is submitted:
# tessellation, stroking, the thin-stroke rule, the batching. It needs no device,
# so it is measured even where every backend was skipped, and it closes a blind
# spot this file's own header used to name and guess the size of. The guess was
# the wrong shape rather than the wrong size. A frame of mixed content builds in
# 0.018 ms against 13.9 to draw, which is the tenth of a per cent the old note
# expected. A *stroked path* builds in 0.739 and draws in 1.184 -- sixty-two per
# cent of the draw, on the row that is cheapest to draw. Timing only the drawing
# made the stroker look free.
#
# **These numbers require `scaling_governor` to be `performance`, and that setting
# does not survive a reboot.** Under `ondemand`, which is this board's default, the
# recording rows land in two or three states: the distance field at 0.136 and
# 0.154, the four-sample tessellation at 0.268 and 0.341, and single spikes of
# plus forty per cent on two more. Pinned, every one of them holds inside one per
# cent over four runs. The reason is not subtle -- 1.5 GHz to 2.4 is a factor of
# 1.6, and every gap observed sits inside it -- but nothing in the output said
# which governor had produced a number, so `cargo xtask bench` now prints it in
# its header. A run whose header says `ondemand` is not comparable with this file.
#
# Two GPU rows moved with the governor as well, and they are the two with enough
# processor work in them to notice: the stroked path, which is the cheapest frame
# here, went 1.226 to 1.184 on Vulkan and 1.351 to 1.319 on GLES, and the
# twelve-draw frame went 14.150 to 13.908 and 14.831 to 14.748. Everything that is
# GPU-bound did not move at all. The GLES rows' occasional four and five per cent
# outliers went with it too, which had been put down to contention.
#
# The two tessellated recording rows read the same number to a thousandth, and
# should: antialiasing decides how many samples the pass takes and not what
# geometry is built. Under `ondemand` they read 0.341 and 0.244 and the difference
# was the clock. `the_two_tessellated_rows_build_the_same_recording` now holds them
# together, so a future divergence means something began depending on the sample
# count at record time.
#
# Tolerances for the new rows are measured: five per cent covers the three per cent
# each moved across five pinned runs. The frame's build carries ten, and not
# because it is noisy -- it read 0.018 in every run -- but because eighteen
# microseconds is eighteen units of the thousandth this file stores, so one unit of
# granularity is five and a half per cent and a tighter tolerance would be gating
# the format rather than the renderer.
#
# **The Vulkan bimodality is not claimed to be gone.** It would be the obvious
# thing to conclude: it is a processor-frequency-shaped effect on the row with a
# hundred and sixty draws, the older notes above could not establish a mechanism,
# and none of the four pinned runs showed the slow state where nine unpinned ones
# showed it five times. But four runs against a coin that lands slow half the time
# is not evidence, and the four Vulkan tolerances are therefore left as wide as
# they were. Settling it wants eight to ten pinned runs, which is worth doing and
# was not done here.
#
# Re-recorded a fifth time on 2026-09-18, for two new rows rather than for a
# change in the renderer. `stroked field, 1 sample` and `stroked path, 1 sample`
# are the same hundred and sixty rounded rectangles *stroked* at four device
# pixels, once through the analytic field and once through the tessellator, so
# strokes get the route comparison the fills already had. Nothing timed a stroke
# before this, which is how a shader change that cost two and a half per cent
# reached a release.
#
# What they measure, and what they do not. Only `execute` is timed and the
# recording is built before the clock starts, so the stroke *rules* -- a width
# widened to a pixel, the alpha that pays for it, a hairline snapped onto one --
# are not in these numbers. They live in the recording. What is here is what an
# outline costs to draw: the field's fragment work, and the geometry the stroker
# emits.
#
# The stroked path is the cheapest row in the file, at a fifth of the filled
# tessellated one, and that is not a mistake. An outline four pixels wide covers
# a band around each shape where a fill covers the whole interior, and fragments
# are what a tiler spends. Stroking costs more to *build* and far less to draw.
#
# Two new tolerances, measured over ten runs. The Vulkan stroked field joins the
# bimodal family: it separates into 9.432/9.432/9.437 and
# 9.790/9.794/9.795/9.800/9.806/9.818, a gap of 3.9 per cent, so it carries 5.0
# like the distance field beside it. Both stroked path rows hold one cluster
# about one per cent wide and carry 2.0. The GLES stroked field holds
# 9.475-9.515 across six runs and spiked once to 9.905 in a seventh whose other
# rows were clean, so it carries 5.0 as well rather than reporting that spike as
# a regression every few runs.
#
# **The Vulkan frame row moved and the renderer did not.** It was 13.955 and is
# 14.135, up 1.3 per cent, and the commit that re-recorded this touches
# `xtask/src/bench.rs` and nothing else -- no renderer code at all. Two more
# configurations now run before the frame in the same process, and that is what
# it responded to. Checked for heat and it is not heat: a run from a cold board
# at 58 C gives 14.135 to the thousandth, the same as one taken at 66 C. So this
# row's number is not portable across changes to the bench's own shape, which is
# worth knowing before reading a future move in it as a renderer's doing.
#
# One older claim above is now doubtful. It says all four Vulkan rows land in one
# of two states, the frame moving 1.6 per cent with them. Over nine clean runs
# here the frame held 14.138-14.202 -- one cluster, less than half a per cent --
# while the distance field and the stroked field each separated cleanly. So with
# five rows the frame did not follow them. Not asserted as a change in the
# board, since nothing here re-tested the older measurement's conditions; noted
# so the next person to read that paragraph knows it was not reproduced.
#
# Checked three times on a quiet board after recording, from 58 C, all three
# passing. Two of the three landed in the slower Vulkan state and stayed inside
# the tolerances that exist for it -- distance field +4.0 and +4.1, stroked field
# +3.9 and +4.0 against 5.0 -- and the third landed in the faster one and held
# every row to two tenths. GLES held every row to three tenths in all three. So
# the two new tolerances are sized by what the board does rather than by taste.
#
# **A quiet board is a precondition and this one is not always quiet.** The first
# attempt at that verification failed three times over, with one run's GLES group
# reading 57 and 71 per cent slow while its Vulkan group was perfect to four
# tenths. The cause was `homescreen`, a Flutter embedder, running at about a
# sixth of a core and drawing through the same V3D: not a service, so nothing
# restarts it and nothing announces it. `pgrep homescreen` before a run is worth
# more than any amount of re-running, and a group of rows that comes back several
# per cent slow together while the other device's group is clean is what it looks
# like.
#
# Contaminated sections are excluded from all of the above, and the rule is
# stated rather than applied quietly: a device's whole group of rows comes back
# several per cent slow together, the frame row worst, which is the descheduling
# the epilogue describes. Of ten runs, one had a bad Vulkan group and three a
# bad GLES group, and they are dropped per group rather than per run because the
# two devices are measured in sequence and only one was ever affected at a time.
#
# Re-recorded a fourth time on 2026-09-01, for one commit: `1f64a28`, which
# folded a rounded rectangle's fill and its outline into one expression. It was
# written for the duplication rather than for the clock, and the shader it left
# is 431 bytes of GLSL and 68 SPIR-V words smaller. What that bought, and cost:
#
#   vulkan full frame      14.444 -> 13.955   -3.4%   the row an interface pays
#   vulkan tessellated x4   4.481 ->  4.522   +0.9%
#   vulkan tessellated x1   3.661 ->  3.691   +0.8%
#   gles   full frame      14.540 -> 14.835   +2.0%
#
# Neither distance-field row moves, which is the part worth pausing on: that is
# the path the folded function serves. Whatever the four rows above are
# responding to, it is not the arithmetic that changed -- it is the size and
# shape of the program around it, differently on each backend's compiler. The
# same conclusion `docs/on-a-board.md` reaches from the other direction.
#
# `e9833c8` is in the same span and measures at nothing. It deleted a function
# nothing ever called -- 207 bytes of GLSL, 78 SPIR-V words -- and four runs of
# it against four of `1f64a28` alone are indistinguishable on all eight rows.
# The commit declined to claim a gain; this is the measurement that says there
# was none.
#
# Attributed by benching three commits over sixteen runs, four a side in the
# faster Vulkan state: `ce3b31f` before both shader commits reproduces the
# previous numbers exactly, `1f64a28` alone reproduces the ones below, and the
# tip is indistinguishable from `1f64a28`.
#
# Re-recorded a third time on 2026-09-01, and this one is a correction rather
# than a change. The numbers below had been recorded at `01119dd` and the
# checked-at line then moved forward to `259db30`, on the reasoning that a hash
# cannot be recorded inside the object it names. That reasoning is right and the
# commit it landed on was not: `259db30` changes what the bench times, and its
# own message says so -- "the distance-field benchmark came back four per cent
# faster on the Pi 5". So the line named a commit whose state had never passed,
# and the gate counted from the wrong place.
#
# Established by benching three commits, three runs each, on one board in one
# sitting. `01119dd` reproduces every row of the old numbers to within four
# tenths of a percent and passes. `259db30` moves four of the eight rows and
# fails on one. `5dfc1ff`, the eight commits since, is level with `259db30`
# everywhere -- the largest difference between them is a fifth of a percent, and
# the GLES analytic-stroke row reads 8.926/8.914/8.930 against 8.903/8.909/8.917,
# clusters that overlap.
#
# So the whole of the movement below belongs to `259db30`, which drew a stroke
# as the difference of two offset shapes:
#
#   vulkan distance field   9.225 -> 8.857   -4.0%   what its message claimed
#   gles   distance field   8.714 -> 8.919   +2.4%   what nobody noticed
#   vulkan full frame      14.623 -> 14.442  -1.2%
#   gles   full frame      15.033 -> 14.530  -3.3%
#
# The tessellated rows do not move, which is the check on that attribution: the
# change is to the analytic stroke and those four rows never take it.
#
# One row got slower and it is recorded rather than argued away. The commit is a
# net win on this board -- both full-frame rows and the Vulkan stroke row are
# faster, and the frame is what an interface pays -- but the GLES stroke row
# costs 2.4 per cent for it, and the two backends running the same WGSL is
# exactly why that is worth writing down rather than averaging out. It went
# unseen because the Vulkan row it is paired with carries a five per cent
# tolerance for its own bimodality, and the run that would have failed on GLES
# was never made.
#
# Re-recorded twice before that on 2026-09-01, for three fixes of one kind: work
# every fragment did that its own draw had not asked for.
#
# `shade` dispatched on the material kind with a chain of `if`s, so a fragment
# paid two float comparisons for each kind ahead of its own and a solid color
# fell through all fourteen. `fs_main` called `blend_tint` unconditionally --
# twenty-nine modes and the non-separable tail behind them -- to arrive at the
# one multiply that `Modulate`, the mode a draw that asked for nothing gets,
# actually means. And `dithered` computed which kinds dither before the
# amplitude test that discards the answer for every draw that is not a
# gradient's.
#
# Against what this file held on 2026-08-26, three runs a side agreeing to a
# hundredth of a millisecond: the tessellated rows fell by about three quarters,
# 13.661 to 3.694 and 15.976 to 4.523; the distance-field rows by a third; the
# mixed frame by a third and a quarter.
#
# Every pixel of all four hundred and twenty-six catalog and corpus scenes is
# byte-identical across both changes, checked by hashing each scene before and
# after. That check is not optional here and no test in the tree replaces it:
# both backends run the same WGSL, and the software reference is lavapipe
# running it too, so a shared mistake would pass every comparison the suite
# makes.
#
#
# device	configuration	median in milliseconds	[tolerance %]
recording	distance field, 1 sample	0.099	5.0
recording	tessellated, 4 samples	0.235	5.0
recording	tessellated, 1 sample	0.240	5.0
recording	stroked field, 1 sample	0.098	5.0
recording	stroked path, 1 sample	0.739	5.0
recording	full frame, mixed content	0.018	10.0
vulkan:0 V3D 7.1.7.0	distance field, 1 sample	8.852	2.0
vulkan:0 V3D 7.1.7.0	tessellated, 4 samples	4.525	2.0
vulkan:0 V3D 7.1.7.0	tessellated, 1 sample	3.688	2.0
vulkan:0 V3D 7.1.7.0	stroked field, 1 sample	9.431	2.0
vulkan:0 V3D 7.1.7.0	stroked path, 1 sample	1.184	3.0
vulkan:0 V3D 7.1.7.0	full frame, mixed content	13.908	2.0
gles V3D 7.1.7.0	distance field, 1 sample	8.899
gles V3D 7.1.7.0	tessellated, 4 samples	5.460
gles V3D 7.1.7.0	tessellated, 1 sample	3.644
gles V3D 7.1.7.0	stroked field, 1 sample	9.483
gles V3D 7.1.7.0	stroked path, 1 sample	1.319	2.0
gles V3D 7.1.7.0	full frame, mixed content	14.748
